export default function AnalyticsPage({ data }) { return <section className="analytics-page"><div className="page-title"><div><span className="eyebrow">PERFORMANCE INTELLIGENCE</span><h2>Analytics</h2><p>See what is resonating, then teach the next agent run to do more of it.</p></div><button className="secondary-button">Last 30 days ▾</button></div><div className="analytics-stats">{Object.entries(data.summary).map(([name, value]) => <article key={name}><small>{name.replace(/([A-Z])/g, ' $1').toUpperCase()}</small><b>{typeof value === 'number' && name !== 'contentGenerated' && name !== 'scheduled' && name !== 'published' ? `${value}%` : value}</b><em>↑ 12.4%</em></article>)}</div><div className="analytics-grid"><section className="section-card performance"><div className="section-heading"><div><span className="eyebrow">PLATFORM PERFORMANCE</span><h3>Engagement, by channel</h3></div></div><div className="bar-chart">{data.platforms.map(platform => <div className="bar-row" key={platform.name}><span>{platform.name}</span><div><i style={{ width: `${platform.engagement * 9}%`, background: platform.color }} /></div><b>{platform.engagement}%</b></div>)}</div></section><section className="section-card insight-card"><span>✦</span><div><span className="eyebrow">AI RECOMMENDATION</span><h3>Keep making the useful thing.</h3><p>{data.insight}</p><button>Use this insight in next brief →</button></div></section></div></section>; }
