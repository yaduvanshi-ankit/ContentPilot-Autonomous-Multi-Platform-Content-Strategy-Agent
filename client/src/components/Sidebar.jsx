import Brand from './Brand';
const links = [['overview', '⌂', 'Overview'], ['discover', '◉', 'Discover'], ['calendar', '▦', 'Content calendar'], ['content', '▤', 'My content'], ['analytics', '◔', 'Analytics']];
export default function Sidebar({ page, setPage, user, logout }) { return <aside className="sidebar"><Brand /><nav>{links.map(([id, icon, label]) => <button className={`nav-item ${page === id ? 'active' : ''}`} onClick={() => setPage(id)} key={id}><i>{icon}</i>{label}</button>)}</nav><div className="sidebar-bottom"><div className="upgrade-card"><span>✦</span><b>Unlock your full team</b><p>Get unlimited agent runs.</p><button>View plans →</button></div><button className="nav-item"><i>⚙</i>Settings</button><button className="profile" onClick={logout}><div className="profile-avatar">{user.name.split(' ').map(item => item[0]).join('').slice(0, 2)}</div><div><b>{user.name}</b><small>{user.plan} plan</small></div><span title="Sign out">↗</span></button></div></aside>; }
