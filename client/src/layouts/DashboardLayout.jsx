import Sidebar from '../components/Sidebar';
export default function DashboardLayout({ page, setPage, user, logout, children, onBrief }) { return <div className="app-shell"><Sidebar page={page} setPage={setPage} user={user} logout={logout} /><main className="dashboard"><header className="topbar"><div><p className="greeting">AUTONOMOUS CONTENT WORKSPACE</p><h1>{page === 'overview' ? <>Good morning, {user.name.split(' ')[0]} <span>✦</span></> : page[0].toUpperCase() + page.slice(1)}</h1></div><div className="top-actions"><button className="icon-button">⌕</button><button className="icon-button notification">♧<i /></button><button className="new-button" onClick={onBrief}>+ New content</button></div></header>{children}</main></div>; }
